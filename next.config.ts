import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  // experimental: {
  //   appDir: true, // Active le support pour `app/`
  // },
};

export default nextConfig;
